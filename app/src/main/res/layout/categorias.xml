<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".categorias.Categorias">

    <ImageView
        android:id="@+id/btn_atras_in"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="15dp"
        android:src="@drawable/ic_flecha_at"
        />

    <ImageView
        android:id="@+id/Logo_app"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/logo_app"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.947"
        app:layout_constraintStart_toEndOf="@id/btn_atras_in"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/input_buscar"
        android:layout_width="350dp"
        android:layout_height="50dp"
        android:background="@drawable/input"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btn_atras_in"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:drawableEnd="@drawable/inc_buscar"
        android:drawablePadding="10dp"
        android:hint="@string/buscar"
        android:inputType="text"
        android:paddingEnd="20dp"
        android:paddingStart="20dp"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_inventario"
        android:layout_width="match_parent"
        android:layout_height="590dp"
        tools:listitem="@layout/items_categorias"
        app:layout_constraintTop_toBottomOf="@+id/input_buscar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_agregar"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="64dp"
        app:backgroundTint="#3241DB"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.954"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/icn_agregar" />

</androidx.constraintlayout.widget.ConstraintLayout>
