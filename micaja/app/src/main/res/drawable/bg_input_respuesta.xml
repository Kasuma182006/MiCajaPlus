<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo del input -->
    <solid android:color="#C6B5F6" />

    <!-- Radios para que quede redondeado -->
    <corners android:radius="100dp" />

    <!-- Padding interno (si usas CardView o contenedor, puede sobrar) -->
    <padding
        android:left="30dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp" />
</shape>
